/* exported setValue */
// define function
function setValue(object, key, value) {
  // put in new data that has the "value" as the property assigned to the key in the storage "object"
  object[key] = value;
}
