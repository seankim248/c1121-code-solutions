/* exported lastChars */
// define function
function lastChars(length, string) {
  // push out the cut string from the last character to the "length" index character
  return string.slice(-length);
}
